---
# Landing page.
#
# The page itself carries no content: every section is composed by
# _layouts/index.html from the data files in _data/ (homepage.yml,
# deployments.yml, databases.yml, adopters.yml, ai.yml). Anything written
# below the front matter is rendered after the last section.

layout: index
title: Debezium
description: >-
  Debezium is an open source distributed platform for change data capture.
  Point it at your databases and stream every insert, update and delete as it
  is committed, to Kafka, to Debezium Server, or straight into your own
  application.
---