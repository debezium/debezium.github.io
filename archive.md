---
# The archive lists every post grouped by year and month, so it is a single
# page by design.
#
# `pagination.enabled` used to be set here, which made jekyll-paginate-v2
# generate /archives/page/2/ through /archives/page/36/ - 35 extra pages, each
# rendering the complete archive again, because the layout iterates site.posts
# and never looks at `paginator`.

layout: archive
title: Archive
permalink: /archives/
---
