---
# Content lives in _data/why.yml (overview) and _data/homepage.yml (the four
# value cards, shared with the landing page so the two cannot drift).
layout: why
title: Why Debezium
permalink: /why/
description: >-
  Debezium is change data capture for the databases you already run. It reads
  committed changes from the transaction log and turns them into events your
  systems can react to, without changing the applications that write the data.
---
