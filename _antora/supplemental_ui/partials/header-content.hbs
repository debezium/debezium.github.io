{{!--
    The top bar is generated from _data/navigation.yml so the documentation and
    the rest of debezium.io always offer the same menu. See dbz-navbar.hbs and
    `generate_antora_nav` in the Rakefile.
--}}
{{> dbz-navbar}}
