---
# Releases overview. A single page listing every series, so no pagination.
#
# `pagination.enabled` used to be set here, which made jekyll-paginate-v2
# generate /releases/page/2/ onwards - 35 extra pages, each rendering the whole
# overview again, because the layout never looks at `paginator`.

layout: releases
title: Debezium Releases Overview
permalink: /releases/
---
