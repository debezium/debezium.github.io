---
layout: post
title:  Debezium 3.6.0.Beta2 Released
date:   2026-06-15
tags:   [ releases, mongodb, mysql, mariadb, postgres, sqlserver, cassandra, oracle, db2, vitess, outbox, spanner, jdbc, informix, ibmi, cockroachdb, yashandb ]
author: ccranfor
extraClasses: release-v2
---

As the Debezium 3.6 development cycle continues, we're pleased to announce **Debezium 3.6.0.Beta2**, a feature-rich release with significant improvements across the platform.

This release introduces MySQL GTID tags support, a new Debezium CLI, and Spanner UUID support.
The Quarkus Extensions gain column change filtering for both single and batch event capturing, a start and stop lifecycle API, and multi-engine improvements.
MySQL and MariaDB connectors now offer customizable zero-date fallback values, the Platform Helm Chart adds OpenTelemetry Collector deployment, and Apicurio has been upgraded to version 3.2.5.

Read on for the full details!

+++<!-- more -->+++

* link:#new-features-and-improvements[New features and improvements]
* link:#other-changes[Other changes]

[id="new-features-and-improvements"]
== New Features and Improvements

The following covers all new features and improvements, grouped by connector component area.

[cols="25%,25%,25%,25%"]
|===

|link:#new-features-and-improvements-debezium-core[Debezium Core]
|link:#new-features-and-improvements-debezium-mariadb[Debezium for MariaDB]
|link:#new-features-and-improvements-debezium-mongodb[Debezium for MongoDB]
|link:#new-features-and-improvements-debezium-mysql[Debezium for MySQL]

|link:#new-features-and-improvements-debezium-quarkus-extension[Debezium Quarkus Extensions]
|link:#new-features-and-improvements-debezium-operator[Debezium Operator]
|link:#new-features-and-improvements-debezium-platform[Debezium Platform]
|link:#new-features-and-improvements-debezium-cli[Debezium CLI]

|link:#new-features-and-improvements-debezium-cockroachdb[Debezium for CockroachDB]
|link:#new-features-and-improvements-debezium-spanner[Debezium for Spanner]
|
|

|===

[id="new-features-and-improvements-debezium-core"]
=== Debezium Core

==== Apicurio upgrade to version 3.2.5

The Apicurio Registry dependency has been upgraded to version 3.2.5.
Apicurio 2.6 is being deprecated, and this upgrade ensures that Debezium stays current with the actively maintained 3.x line for schema registry integration.
(https://github.com/debezium/dbz/issues/1409[dbz#1409])

==== Default values in schema descriptor API response

The schema descriptor API now includes default values for configuration fields when a default is available.
Previously, defaults were only mentioned in the field description text, making it difficult for UI tooling to pre-fill connector forms with appropriate values.
(https://github.com/debezium/dbz/issues/1941[dbz#1941])

[id="new-features-and-improvements-debezium-mariadb"]
=== Debezium for MariaDB

==== Customizable zero-date fallback values

The MySQL and MariaDB connectors now support customizable fallback values for zero-date entries (`0000-00-00`) on a per-type basis.
Previously, zero dates in `NOT NULL` columns were always coerced to `1970-01-01`, making legitimate epoch timestamps indistinguishable from coerced zero dates in downstream analytics.
Four new optional properties (`zero.date.fallback.value`, `.date`, `.datetime`, `.timestamp`) allow users to specify distinct sentinel values for each temporal type using a cascading lookup, enabling cleaner downstream filtering.
This release also fixes a related bug where the streaming path did not apply the configured sentinel to `TIMESTAMP` columns.
(https://github.com/debezium/dbz/issues/1912[dbz#1912])

[id="new-features-and-improvements-debezium-mongodb"]
=== Debezium for MongoDB

==== Error handling for missing additional fields in MongoEventRouter

The `collection.fields.additional.error.on.missing` configuration property is now properly honored by the MongoDB event router.
Previously, this setting was silently ignored, causing `NullPointerException` errors when an additional field specified in the placement configuration did not exist in the change event, even when missing-field errors were explicitly disabled.
(https://github.com/debezium/dbz/issues/1342[dbz#1342])

[id="new-features-and-improvements-debezium-mysql"]
=== Debezium for MySQL

==== GTID tags support

Starting with MySQL 8.3, GTIDs can include an optional tag in the form `<serverID>:<tag>:<TxID>`.
The MySQL connector and the underlying binlog client now correctly parse and handle tagged GTIDs, ensuring compatibility with this newer GTID format.
(https://github.com/debezium/dbz/issues/1200[dbz#1200])

[id="new-features-and-improvements-debezium-quarkus-extension"]
=== Debezium Quarkus Extensions

==== Column change filtering for single event capturing

The Debezium Quarkus extension now supports filtering change events based on which columns were modified.
When only a subset of columns is relevant to downstream consumers, this filter suppresses events that do not include changes to the specified columns, reducing unnecessary processing and improving pipeline efficiency.
(https://github.com/debezium/dbz/issues/1081[dbz#1081])

==== Start and stop lifecycle API

A new public, CDI-exposed lifecycle controller enables manual start and stop control of the Debezium Engine within the Quarkus extension.
This allows users to prevent auto-start and integrate external distributed locks for single-active hot-standby failover scenarios.
(https://github.com/debezium/dbz/issues/1662[dbz#1662])

==== Preserved event ordering in batch processing

The batch processing mode in the Quarkus extension previously reordered events by destination, which could be harmful in scenarios where the original event order matters.
The destination field on `CapturingEvents` has been deprecated in favor of a new destination field on `BatchEvent`, and the default ordering logic has been removed from the change consumer, preserving the original event sequence.
(https://github.com/debezium/dbz/issues/1984[dbz#1984])

==== Column change filtering for batch event capturing

Building on the single event column change filtering support, this enhancement extends the same capability to batch event capturing in the Quarkus extension.
Batch consumers can now define column-level filters to suppress events that do not include changes to the specified columns, maintaining consistency between single and batch processing modes.
(https://github.com/debezium/dbz/issues/1991[dbz#1991])

==== Multi-engine heartbeat event support

Heartbeat events are now supported in multi-engine Quarkus deployments.
Previously, heartbeat functionality was limited to the default engine, and multi-engine configurations could encounter `NoSuchElementException` errors in certain scenarios.
(https://github.com/debezium/dbz/issues/2006[dbz#2006])

==== Engine filtering and multi-connector support

The Quarkus extension now supports running multiple connectors side by side (e.g., PostgreSQL and MySQL) with the ability to selectively start only a subset of engines.
Previously, the startup path obtained a single connector registry and iterated its engines directly, with no mechanism to aggregate engines from multiple connectors or apply filtering rules during startup.
(https://github.com/debezium/dbz/issues/2007[dbz#2007])

[id="new-features-and-improvements-debezium-operator"]
=== Debezium Operator

==== Extra volumes and volume mounts support

The Debezium Platform Helm chart now supports mounting additional volumes and volume mounts into the conductor container.
This allows users to provide files such as custom truststores at runtime, serving as a practical workaround until the platform natively manages certificates and similar artifacts.
(https://github.com/debezium/dbz/issues/1994[dbz#1994])

[id="new-features-and-improvements-debezium-platform"]
=== Debezium Platform

==== OpenTelemetry Collector deployment

The Debezium Platform Helm chart now includes optional deployment of an OpenTelemetry Collector and Prometheus.
The collector aggregates metrics from all Debezium Server instances and exposes them in Prometheus format, supporting three deployment scenarios: installing the Prometheus Operator, using an existing Prometheus Operator, or connecting to a vanilla Prometheus instance.
(https://github.com/debezium/dbz/issues/1897[dbz#1897])

[id="new-features-and-improvements-debezium-cli"]
=== Debezium CLI

==== Unified command-line interface

A new unified command-line interface for Debezium provides developers and operators with a streamlined way to create, manage, and monitor CDC pipelines across heterogeneous data systems.
The CLI simplifies pipeline lifecycle operations, exposes monitoring insights, and enables consistent workflows for both local development and production environments.
(https://github.com/debezium/dbz/issues/1955[dbz#1955])

[id="new-features-and-improvements-debezium-cockroachdb"]
=== Debezium for CockroachDB

==== Changefeed splitting across tables

A new `cockroachdb.changefeed.max.tables.per.changefeed` configuration option allows users to split captured tables across multiple changefeeds to avoid performance coupling.
CockroachDB advises against watching too many tables with a single changefeed because a slow table can degrade the entire feed.
When set to a positive value, the connector automatically distributes tables into balanced groups while preserving per-group reuse detection for idempotent restarts.
(https://github.com/debezium/dbz/issues/2014[dbz#2014])

[id="new-features-and-improvements-debezium-spanner"]
=== Debezium for Spanner

==== UUID data type support

The Spanner connector now supports the UUID data type, which Google introduced for Cloud Spanner in January 2026.
Applications no longer need to use `STRING(36)` columns as a workaround and can take advantage of native UUID support in their schemas.
(https://github.com/debezium/dbz/issues/1980[dbz#1980])

[id="other-changes"]
== Other fixes

// [Debezium Platform]
* Update ReactFlow to latest XYZFlow (https://github.com/debezium/dbz/issues/1424[dbz#1424])
// [Debezium for MariaDB]
* Failover restart can trigger unknown schema error despite non-empty schema history (https://github.com/debezium/dbz/issues/1672[dbz#1672])
// [Debezium for Oracle]
* OpenLogReplicator streaming may skip all events when resuming from checkpoint without index (https://github.com/debezium/dbz/issues/1756[dbz#1756])
// [Debezium for IBMi]
* Incremental snapshot is not resumed after a restart (https://github.com/debezium/dbz/issues/1861[dbz#1861])
// [Debezium for Spanner]
* Credential leakage in logs — sensitive config values logged in plaintext (https://github.com/debezium/dbz/issues/1869[dbz#1869])
// [Debezium for MongoDB]
* Connector fails with array.encoding set to document (https://github.com/debezium/dbz/issues/1901[dbz#1901])
// [Debezium for PostgreSQL]
* Negative timestamp not handled correctly during snapshot (https://github.com/debezium/dbz/issues/1916[dbz#1916])
// [Debezium Quarkus Extensions]
* Add backward compatibility for datasource configuration (https://github.com/debezium/dbz/issues/1936[dbz#1936])
// [Debezium Quarkus Extensions]
* Impossible to route default capturing destination for batch processing (https://github.com/debezium/dbz/issues/1943[dbz#1943])
// [dependencies]
* Align Quarkus version across Operator, Server, Platform, and Extensions to 3.33.1.1 (https://github.com/debezium/dbz/issues/1951[dbz#1951])
// [Debezium CLI]
* Set up Debezium CLI project with Picocli and JBang (https://github.com/debezium/dbz/issues/1956[dbz#1956])
// [Debezium Quarkus Extensions]
* Avoid overriding configuration with Agroal (https://github.com/debezium/dbz/issues/1985[dbz#1985])
// [Debezium Quarkus Extensions]
* Disable compatibility mode (https://github.com/debezium/dbz/issues/1986[dbz#1986])
// [Debezium for Spanner]
* Memory leak in SpannerBaseSourceTask where full SourceRecords are retained until offset commit (https://github.com/debezium/dbz/issues/1992[dbz#1992])
// [Debezium for Spanner]
* Partition offsets resolved one-by-one without timeout protection on batch path (https://github.com/debezium/dbz/issues/1993[dbz#1993])
// [Debezium for MariaDB]
* Possible regression in binlog snapshot consistency (https://github.com/debezium/dbz/issues/1995[dbz#1995])
// [Debezium for Cassandra]
* Connector fails to start with a template NullPointerException (https://github.com/debezium/dbz/issues/1996[dbz#1996])
// [Debezium Quarkus Extensions]
* Add new Debezium services as reflective classes in EngineProcessor (https://github.com/debezium/dbz/issues/1997[dbz#1997])
// [Debezium for CockroachDB]
* Event deduplication collides across schemas when using unqualified table names (https://github.com/debezium/dbz/issues/1998[dbz#1998])
// [Debezium for CockroachDB]
* Default enriched_properties to source and treat the value as a passthrough (https://github.com/debezium/dbz/issues/1999[dbz#1999])
// [Debezium for CockroachDB]
* Connector accepts envelope values it cannot parse (https://github.com/debezium/dbz/issues/2000[dbz#2000])
// [Debezium for Oracle]
* STORE-syntax DDL fails to parse due to quoted segment name (https://github.com/debezium/dbz/issues/2005[dbz#2005])
// [Debezium for Spanner]
* QuantileMeter spawns unnecessary background threads for percentile computation (https://github.com/debezium/dbz/issues/2010[dbz#2010])
// [Debezium Platform]
* Connection creation fails with HTTP 400 when using SQS connection type (https://github.com/debezium/dbz/issues/2011[dbz#2011])
// [Debezium for CockroachDB]
* Connector forces a dot separator on the topic prefix and ignores topic_name in the sink URI (https://github.com/debezium/dbz/issues/2013[dbz#2013])
// [Debezium Platform]
* Add Cypress-based end-to-end tests for missing user flows (https://github.com/debezium/dbz/issues/2016[dbz#2016])
// [Debezium for YashanDB]
* Clean up and align unused SourceInfo fields and event examples (https://github.com/debezium/dbz/issues/2017[dbz#2017])
// [Debezium for YashanDB]
* Incorrect official website link for the YashanDB connector (https://github.com/debezium/dbz/issues/2021[dbz#2021])
// [Debezium for CockroachDB]
* Changefeed consumer cannot connect to a secured (TLS/mTLS/SASL) Kafka broker (https://github.com/debezium/dbz/issues/2023[dbz#2023])
// [Debezium for YashanDB]
* Incorrect database version in YashanDB connector release documentation (https://github.com/debezium/dbz/issues/2029[dbz#2029])

In total, https://github.com/orgs/debezium/projects/5/views/6?query=sort%3Aupdated-desc+is%3Aopen&filterQuery=status%3AReleased+iteration%3A3.6.0.Beta2[51 issues] were resolved in Debezium 3.6.0.Beta2.
The list of changes can also be found in our link:/releases/3.6/release-notes#release-3.6.0-beta2[release notes].

A big thank you to all the contributors from the community who worked diligently on this release:
 +
https://github.com/Alex-pvl[Aleksandr Pavlyuk],
https://github.com/atorik[Atsushi Torikoshi],
https://github.com/Naros[Chris Cranford],
https://github.com/kmos[Giovanni Panice],
https://github.com/indraraj[Indra Shukla],
https://github.com/jpechane[Jiri Pechanec],
https://github.com/KMohnishM[Kodukulla Mohnish Mythreya],
https://github.com/nrkljo[Lars M. Johansson],
https://github.com/mfvitale[Mario Fiore Vitale],
https://github.com/mcrauwel[Matthias Crauwels],
https://github.com/minleejae[Minjae Lee],
https://github.com/pxcamus[Philippe Camus],
https://github.com/pypsink[Pierre-Yves Péton],
https://github.com/roldanbob[Robert Roldan],
https://github.com/100maygupta[Saumay Gupta],
https://github.com/sauravkumarrr[Saurav Kumar],
https://github.com/Tay-Day[Tayler Dunn],
https://github.com/vincadrn[Vincentius Adrian],
https://github.com/vsantonastaso[Vincenzo Santonastaso],
https://github.com/viragtripathi[Virag Tripathi],
https://github.com/vjuranek[Vojtech Juranek],
https://github.com/yashisrivastava-confluent[Yashi Srivastava],
https://github.com/yuanglili[Yuang Li],
https://github.com/Tlinian[林石培], and
https://github.com/OctoberWithYou[梁嘉成]!
