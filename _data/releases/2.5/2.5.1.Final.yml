date: 2024-01-30
version: "2.5.1.Final"
stable: true
summary: Post-images support for MongoDB; Oracle and MySQL grammar fixes; POstgreSQL heartbeats in WAL searching phase; Correct default character values for SQL Server; Dbeium Server sinks correctly ignore tombstone events if they are not supported; HTTP sink retries on GOAWAY
# announcement_url:

