date: 2024-03-24
version: "3.1.0.CR1"
stable: false
summary: BYTES are mapped as VARBINARY in SQL Server sink; Logging of all potentially sensitive data was centralized and can be configured via single category; RabbitMQ sink supports routing by key; Vitess supports handling of keypace heartbeats; Vitess supports mapping of temporal datatypes into strings; Helm charts were centralized into a single point
announcement_url: /blog/2025/03/24/debezium-3-1-cr1-released/
