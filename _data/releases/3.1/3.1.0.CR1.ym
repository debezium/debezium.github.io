date: 2024-03-24
version: "3.1.0.CR1"
stable: false
summary: BYTES are mapped as VARBINARy in SQL Server sink; Logging of all potentially sensitive data was cnetralised and can be configured via single category; RabbitMQ sink supports routing by key; Vitess supports handling of keypace heartbeats; Vitess supports mapping of temporal datatypes into strings; Helm charts were centrlized into a single point
#announcement_url:
